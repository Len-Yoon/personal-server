# Repository Guidance

## 1. 기본 원칙

- 사용자 안내·진행 보고·질문·완료 보고는 한국어로 작성함.
- 확인한 사실과 미확인 사항을 구분하며, 미확인 사항은 `확인 필요`로 표시함.
- 현재 대화의 명시적 승인과 저장소에 기록된 승인 범위를 확인한 뒤 작업함.
- 동일 대상·동작·범위에 대한 승인은 단계 전환·대화 재개·컨텍스트 요약 이후에도 유지하며 반복 요청하지 않음.
- 후속 승인은 해당 범위에만 적용함. 코드 반영 승인을 운영 배포 승인으로 확대하지 않음.
- 진행 보고 후 승인된 작업을 계속 수행함. 진행 보고를 승인 대기로 전환하지 않음.
- 저장소 내 승인·중단·작업 분담 기준은 이 문서에 모으고 절차 문서는 이를 참조함. 사용자 지시와 상위 지침을 우선하며, 참조 문서의 오래된 문구를 추가 승인 요구로 해석하지 않음.

## 2. 연속 진행과 중단 기준

- 승인된 계획은 구현 → 관련 검증 → 독립 검토 → 승인된 저장소 반영 → 결과 기록 → 다음 단계 순서로 진행함.
- 한 목적의 변경은 한 브랜치·한 PR로 묶음. 해당 변경의 검토 보완·재검증은 같은 PR에서 마무리하고, 독립 배포·롤백 또는 위험 분리가 필요한 경우에만 이유를 명시해 나눔.
- 수정 전에 요청 범위·보존 대상·성공 기준과 관련 검사를 함께 확인함. 요청하지 않은 이미지 교체·크기·비율·배치는 변경하지 않으며, 복원 요청은 기준 버전의 파일과 표시 형식을 함께 대조함.
- 추가 확인은 승인 밖 대상·동작, 새로운 중대한 위험, 파괴적 변경, 조사로 해결되지 않는 필수 정보 누락에 한정함.
- 보안·운영·배포 영향이 있다는 이유만으로 이미 승인된 작업을 중단하지 않음.
- 추가 확인이 필요하면 해당 동작만 보류하고 독립적으로 수행 가능한 승인 작업은 계속함.
- 확인 요청에는 기존 승인과 달라진 점, 필요한 결정, 보류한 동작을 명시함.
- 테스트·CI·검토에서 관련 실패가 발생하면 승인 범위 안에서 원인을 보완함. 실패가 해소되기 전에는 병합하지 않음.
- 동일 원인의 실패에 대한 수정·재실행은 최초 실패 이후 최대 3회로 제한함. 해결되지 않으면 해당 동작을 보류하고 원인과 필요한 결정을 보고함. 실패가 없는 최초 검증·계획된 후속 검증·PR 및 main 검사는 횟수에 포함하지 않으며, 같은 원인의 실패는 단계가 바뀌어도 합산함.
- 진행 보고는 발견한 문제·결정·단계 완료·차단 중심으로 짧게 작성함. CI 대기 이유와 남은 조건은 한 번 설명하고 동일한 대기 보고·완료 예고를 반복하지 않음. 상위 지침이 주기적 보고를 요구하면 필요한 최소 상태만 전달함.

## 3. 운영 보호

- 서버 bootstrap·scheduler·배포 정책은 기본 수정 대상에서 제외함. 구체적으로 승인된 작업만 최소 범위로 변경함.
- Portal을 포함한 일반 앱 코드·테스트·문서의 수정과 저장소 반영은 승인된 개발 범위에서 수행함. Portal 운영 배포·K3s 리소스·PVC·운영 데이터·Secret·Caddy·Tunnel ingress·writer 전환은 일반 개발 승인에 포함하지 않으며 별도 승인 범위를 확인함.
- 위 보호 대상의 변경은 대상·동작이 명시된 별도 승인 범위에서만 수행함. 과거 1회성 승인을 새 작업에 재사용하지 않음.
- active 상태의 K3s 전체 restart, 중복 writer 실행, 정상 서비스의 주기적 재생성은 수행하지 않음.
- 새 sudo 권한이나 외부 자격 증명을 생성하지 않으며, 기존의 제한된 `sudo -n k3s` 권한만 사용함.
- 비밀번호·토큰·Secret·내부 IP·계정명·SSH 개인키 경로를 코드·문서·로그에 저장하거나 출력하지 않음.
- 기존 SSH 키를 삭제·재생성·복제하지 않음. 접속 정보 질문 전에 기존 대화·문서·로컬 SSH 설정과 agent 상태를 읽기 전용으로 확인함.
- 승인된 Tunnel 알림은 Windows Credential Manager에 사전 등록된 자격 증명만 읽어 전송 중 메모리에서 사용함. 비밀값을 파일·환경 변수·로그에 복제하지 않음.
- 자동복구는 명시 승인된 제한 동작만 수행하며, 같은 구성요소가 3회 연속 복구 실패하면 추가 재시작을 중단함.
- 운영 변경 전후 독립 운영 검토와 외부 health 검증을 수행함.

## 4. N100 실행과 상태 보고

- Windows 원격 셸과 WSL Bash 명령 경계를 명확히 구분함. curl은 WSL `curl` 또는 Windows `curl.exe`를 사용함.
- 원격 변경 응답이 불완전하면 즉시 재실행하지 않음. 커밋·실행 이력·리소스 상태를 먼저 조회하여 실제 수행 여부를 확인함.
- 저장소 동기화 전 브랜치·커밋·작업공간 변경·hook 영향·대상 커밋을 확인함. 승인된 fast-forward만 수행하고 강제 reset이나 로컬 변경 덮어쓰기는 하지 않음.
- 저장소 동기화, workflow 실행, 이미지 반입, 리소스 적용, 서비스 재시작은 각각 승인 범위를 확인함.
- 결과는 `저장소 병합 / N100 동기화 / 이미지 반입 / 수동 검증 / 자동 실행 활성화 / 외부 health`로 구분함. 해당하지 않는 단계는 `해당 없음`, 수행하지 않은 단계는 `미수행`으로 표시함.
- Git 병합·workflow 성공·배포 제외를 실제 서비스 배포 완료로 표현하지 않음.
- 운영 검증은 Portal과 변경 대상 서비스의 외부 health를 각각 10초 간격으로 3회 확인함. Portal 주소는 `https://len.pe.kr/health`임.
- Deploy N100 실패 시 분류 값과 실패 단계를 확인함. `blocked_path`는 정책상 배포 제외이며 서비스 장애나 병합 실패로 표현하지 않음.

## 5. 안전 자동배포와 백업

- 안전 자동배포는 허용된 Compose 변경 중 `crawler-worker`, `youtube-memo`, `book-memo`, `car-care-worker`만 대상으로 함.
- Portal·K3s·Secret·PVC·운영 데이터·Caddy·bootstrap·scheduler 변경은 안전 자동배포에서 제외함.
- 신뢰 가능한 runtime marker로 실행 주체를 확인함. K3s 소유 서비스는 Docker 배포·자동복구에서 제외하며, marker 오류 시 관련 변경을 차단함.
- 승인 회피를 위해 workflow를 끄거나 분류 정책·변경 묶음을 조작하지 않음.
- Portal PVC 백업 자동화는 승인된 단일 방식으로만 실행하며 `infra/k8s/tools/portal-pvc-backup-verify.sh --go`를 사용함.
- 백업 자격 증명은 승인된 암호화 저장소 또는 사전 등록된 Kubernetes Secret만 참조함. 평문 파일·Git·환경 변수에 저장하지 않음.
- 백업 자동 실행은 수동 검증의 실제 성공(`portal_pvc_backup=PASS`) 후 활성화함. 실패하면 중지 상태를 유지하고 기존 SRE relay로 결과를 보고함.

## 6. 검증과 작업 분담

- 코드·설정 변경 전 변경 범위·제외 범위·성공 기준을 정리하고 `docs/codex-work-loop.md`를 읽음.
- 변경 전 변경 경로 파일을 준비하고 아래 하네스를 실행하여 결과를 작업에 반영함.
  `python3 scripts/run_change_harness.py --input <변경경로파일> --agent-context`
- 커밋 간 변경 목록은 아래 형식으로 생성하고 하네스에 `--input-format git-name-status-z`를 전달함.
  `git diff --name-status -z --find-renames <기준커밋> HEAD > <변경경로파일>`
- 문서 변경은 링크·이미지 보존·문서 내용을 참조하는 기존 계약 테스트까지 먼저 확인하고, 기능 변경은 관련 서비스·회귀 테스트와 정적 검사·독립 검토를 수행함. 공용 런타임·라이브러리·CI 계약·배포 경로 변경 또는 회귀 위험을 판단하기 어려운 경우 전체 테스트를 수행함.
- 검증 후 실제 결과를 `--check-result <검사명>=success|failure`로 전달하여 하네스를 다시 실행함. 필수 검증 누락이나 차단 상태를 완료로 보고하지 않음.
- 검증 결과에는 대상 커밋 또는 작업 트리 diff·변경 경로·실행 환경·명령·결과를 기록함. 이후 변경의 영향을 받지 않는 유효한 결과는 재사용하고 영향받는 검사만 다시 수행함. 필수 CI는 그대로 실행함.
- 로컬 검증 범위와 저장소의 필수 CI를 구분함. 대기 시간을 줄이려고 필수 검사를 생략·취소하거나 workflow를 임의 변경하지 않음. 문서 전용 CI 등 검사 범위 개선은 별도 변경으로 검토함.
- 무관한 기존 테스트 실패는 원인과 비연관 근거를 독립 검토함. 사용자에게 해당 예외를 승인받으면 진행할 수 있으며, 동일 예외 승인을 반복 요청하지 않음.
- 읽기 전용 조사·기능 영향 없는 문서 변경은 주 에이전트 1명이 수행할 수 있음.
- 기능 변경·버그 수정·리팩터링은 구현 담당과 독립 검토 담당을 분리함. 기능 구현·테스트·설정 파일 수만으로 인원을 늘리지 않음.
- 독립적으로 진행할 구현 작업이 있고 병렬 진행의 이점이 있으면 주 담당·구현·검토의 3명으로 분담함. 공용 계약·데이터 경계 등 위험과 작업 의존성을 기준으로 인원을 정하고 근거를 기록함. 같은 파일을 순차 수정해야 하는 작업은 억지로 분리하지 않음.
- CI/CD·보안·DB 마이그레이션·배포 영향 작업은 전문 검토 에이전트를 필수로 포함해 최대 4명으로 수행함.
- 역할·범위·성공 기준을 먼저 정하고 같은 파일을 동시에 수정하지 않음.
- 모델 선택이 가능한 위임 작업은 조사·단순 구현·테스트에 Luna, 설계·운영·보안·rollback 검토에 Terra를 사용하며 Sol을 기본으로 선택하지 않음.

## 7. 현재 개발 계획과 승인 범위

- `docs/20260921_프로젝트보완_개발계획.md`의 단계별 일반 앱·테스트·문서 변경에 대해 구현·검증·독립 검토·커밋·push·PR·CI 및 Agent Review 확인·병합·다음 단계 진행이 승인됨.
- 해당 범위의 저장소 반영 승인을 단계마다 다시 요청하지 않음.
- 위 최초 승인에는 N100 운영 배포가 포함되지 않음. 이후 특정 작업에 받은 운영 승인은 해당 범위에 적용함.
- 병합 전 자동배포 영향을 확인함. 아직 승인되지 않은 운영 변경이 자동으로 발생한다면 해당 병합만 보류함.
- 완료·소진된 일회성 승인은 [개발 계획의 승인 이력](docs/20260921_프로젝트보완_개발계획.md#완료소진된-일회성-승인-이력)에 기록함. 실행 전 완료 여부를 확인하고 새 작업이나 추가 재실행에 재사용하지 않음.

## 8. 기록과 완료

- 개발 단계 시작 전 개발 계획의 현재 상태를 읽고, 완료 후 변경 파일·검증·검토·PR·병합 커밋·운영 상태·다음 단계를 갱신함.
- 작업 규모에 맞춰 계획·기록을 작성함. 단순 문구·서식 변경은 별도 계획서·보고서를 만들지 않고 PR에 변경 이유·범위·검증 결과를 기록함. 기능·운영 변경의 필요한 설계·검증 기록은 유지함.
- 승인 이력은 대상·허용 동작·제외 범위·완료 여부를 함께 기록함. 상세 경과를 AGENTS.md에 계속 누적하지 않음.
- 완료 보고는 변경 내용, 검증 결과, 실제 운영 적용 여부, 미검증·차단 사항, 역할별 검토 결과를 간결하게 포함함.
- 하위 에이전트를 사용하지 않았다면 사유를 짧게 기록함.
- 병합된 작업은 CI와 해당 배포 결과, 작업공간 무변경을 확인한 즉시 해당 작업의 불필요한 브랜치·worktree를 정리함. 미병합·재사용 예정 작업과 사용자 파일은 보존하며, 정리하지 못한 대상과 이유를 완료 보고에 남김.
- 커밋 메시지는 `유형: 설명` 형식의 한국어를 기본으로 함.
- 시간은 내부 저장·비교에 UTC 시간대 인식 ISO 8601을 사용하고, 사용자 표시·날짜 판단에는 `Asia/Seoul`을 적용함. 표시 형식은 `YYYY-MM-DD HH:MM`으로 함.
- 토큰 절감률을 보고할 때는 실제 JSONL 측정 기록을 `scripts/summarize_token_measurements.py`로 집계함. 바이트 절감률을 토큰 절감률로 표현하지 않음.
