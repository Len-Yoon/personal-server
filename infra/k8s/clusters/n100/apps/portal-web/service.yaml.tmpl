# DRAFT ONLY — NOT FOR APPLY. This NodePort is a future Docker Caddy backend contract only.
apiVersion: v1
kind: Service
metadata:
  name: portal-web
  namespace: personal-server
  annotations:
    draft.personal-server.io/not-for-apply: "true"
    draft.personal-server.io/confirm-required: "Caddy reachability, firewall scope, and external exposure"
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: portal-web
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: http
      nodePort: 30080
