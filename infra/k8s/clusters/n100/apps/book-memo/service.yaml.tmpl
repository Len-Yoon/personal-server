# DRAFT ONLY — NOT FOR APPLY. Internal service contract only; public routing requires separate approval.
apiVersion: v1
kind: Service
metadata:
  name: book-memo
  namespace: personal-server
  annotations:
    draft.personal-server.io/not-for-apply: "true"
    draft.personal-server.io/confirm-required: "private reachability and service ownership"
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: book-memo
  ports:
    - name: http
      protocol: TCP
      port: 8003
      targetPort: http
