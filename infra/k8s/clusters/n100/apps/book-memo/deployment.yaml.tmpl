# DRAFT ONLY — NOT FOR APPLY. Image, data copy, and runtime Secret injection require approval.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: book-memo
  namespace: personal-server
  annotations:
    draft.personal-server.io/not-for-apply: "true"
    draft.personal-server.io/confirm-required: "image digest, data ownership, health endpoint"
spec:
  replicas: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: book-memo
  template:
    metadata:
      labels:
        app.kubernetes.io/name: book-memo
      annotations:
        draft.personal-server.io/not-for-apply: "true"
    spec:
      containers:
        - name: book-memo
          image: __CONFIRM_BOOK_MEMO_IMAGE_REF__
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8003
          envFrom:
            - secretRef:
                name: book-memo-runtime
                optional: false
          env:
            - name: BOOK_MEMO_DB_PATH
              value: /data/book-memo/book_memo.sqlite3
          volumeMounts:
            - name: book-memo-data
              mountPath: /data/book-memo
              readOnly: false
          readinessProbe:
            httpGet:
              path: /health
              port: http
          livenessProbe:
            httpGet:
              path: /health
              port: http
      volumes:
        - name: book-memo-data
          persistentVolumeClaim:
            claimName: book-memo-data-dynamic-draft

