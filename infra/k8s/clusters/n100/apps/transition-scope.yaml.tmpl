# DRAFT ONLY — NOT FOR APPLY. Scope contract for review; no workloads or
# Secret values are created by this file.
apiVersion: v1
kind: ConfigMap
metadata:
  name: first-wave-transition-scope-draft
  namespace: personal-server
  annotations:
    draft.personal-server.io/not-for-apply: "true"
data:
  in-scope: "portal-web,crawler-worker,youtube-memo,book-memo"
  excluded: "system-agent,homeops-executor,car-care-worker,caddy"
  storage-gate: "Dynamic local-path scratch passed on native ext4; app data copy, restore, and one-writer cutover remain approval gates"
  writer-gate: "Exactly one Compose or K3s SQLite writer per service"
  secret-gate: "Approved SOPS/age or Secret Manager seed; no Git plaintext"
