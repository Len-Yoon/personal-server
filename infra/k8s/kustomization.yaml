# DRAFT ONLY — NOT FOR APPLY. This deliberately renders no resources.
# Draft resource examples are never referenced here.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: []
