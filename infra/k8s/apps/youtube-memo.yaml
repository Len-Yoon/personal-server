apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: youtube-memo-data
  namespace: personal-server
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: youtube-memo
  namespace: personal-server
  labels:
    app.kubernetes.io/name: youtube-memo
spec:
  # This manifest is intentionally inert. youtube-memo-prepare.sh replaces the
  # sentinel with an imported immutable digest, while preserving replica 0.
  replicas: 0
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: youtube-memo
  template:
    metadata:
      labels:
        app.kubernetes.io/name: youtube-memo
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: youtube-memo
          image: personal-server-youtube-memo:unconfigured-do-not-run
          imagePullPolicy: Never
          ports:
            - name: http
              containerPort: 8002
          envFrom:
            - secretRef:
                name: youtube-memo-runtime
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: [ALL]
          readinessProbe:
            httpGet:
              path: /health
              port: http
          livenessProbe:
            httpGet:
              path: /health
              port: http
          volumeMounts:
            - name: youtube-memo-data
              mountPath: /data/youtube-memo
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: youtube-memo-data
          persistentVolumeClaim:
            claimName: youtube-memo-data
        - name: tmp
          emptyDir:
            medium: Memory
            sizeLimit: 64Mi
---
apiVersion: v1
kind: Service
metadata:
  name: youtube-memo
  namespace: personal-server
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: youtube-memo
  ports:
    - name: http
      port: 8002
      targetPort: http
